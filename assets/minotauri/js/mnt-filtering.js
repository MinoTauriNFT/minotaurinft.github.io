const filterButtons=document.querySelector("#filter-btns").children,items=document.querySelector(".mnt-rarity-items").children;for(let i=0;i<filterButtons.length;i++)filterButtons[i].addEventListener("click",function(){for(let b=0;b<filterButtons.length;b++)filterButtons[b].classList.remove("active");this.classList.add("active");let c=this.getAttribute("data-target");for(let a=0;a<items.length;a++)items[a].style.display="none",c==items[a].getAttribute("data-id")&&(items[a].style.display="block")})